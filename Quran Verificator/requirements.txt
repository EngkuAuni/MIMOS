# UI and Web Interface
streamlit        # Interactive frontend for the app

# PDF & Image Handling
pdf2image        # Convert PDF pages to images
pillow           # Image loading, resizing, format conversion

# Computer Vision
opencv-python      # Image preprocessing, ORB feature matching

# OCR (Fallback)
kraken       # optimized for Uthmani script
torch==1.13.1      # Use Apple's MPS backend or CPU
# Requires Python 3.8–3.10 for torch==1.10.2 and kraken -- currently using 3.10.18

# Text Similarity
rapidfuzz        # Fast Levenshtein / edit distance for fuzzy text matching

# Core Numerical Operations
numpy          # Used across feature matching, descriptor handling, image arrays

# PDF Export
weasyprint         # (Optional) HTML → PDF export for verification reports
reportlab          # (Alternative) Manual PDF generation if WeasyPrint fails

# HTML Parsing / Cleaning
beautifulsoup4     # Clean up HTML output (e.g., diffs, UI rendering)
html2text   # Converts HTML diff output to plaintext if needed

# (Optional/To Add Later)
# llama-cpp-python      # For running small LLMs locally (e.g., Phi-3, Mistral)
# transformers          # For Qari-OCR or LLM explanation models (if needed)